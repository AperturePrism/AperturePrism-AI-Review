# 安全策略

AperturePrism-AI-Review 处理 GitHub App 私钥、模型 Provider 凭据、OAuth 令牌等敏感信息，安全对我们至关重要。感谢你以负责任的方式报告安全问题。

## 报告漏洞

**请勿在公开 Issue 中披露安全漏洞。** 发现疑似漏洞时，请通过以下方式私密报告：

1. 在仓库「Security」页使用 **Report a vulnerability** 私密上报（推荐）；
2. 或直接联系维护者，并提供尽可能多的细节：受影响组件、复现步骤、影响范围、建议修复。

## 我们承诺

- 在收到报告后尽快确认并评估影响；
- 修复期间与你保持沟通；
- 修复并发布后，在你的同意下给予致谢。

## 范围

- **范围内**：本仓库源码、Docker 镜像打包、部署配置中的安全缺陷（认证绕过、密钥泄露、注入、越权、依赖漏洞等）。
- **范围外**：
  - 未按文档部署导致的自伤配置（如 `WEBUI_API_TOKEN` 未设置、端口暴露公网）；
  - 依赖第三方模型网关 / 中转站自身的安全问题；
  - DoS / 社会工程类攻击。

## 安全最佳实践（部署者）

- 为 GitHub App 私钥、`CREDENTIAL_MASTER_KEY`、数据库密码使用独立随机值，绝不提交到仓库；
- 生产环境务必设置 `WEBUI_API_TOKEN`，并把管理接口置于内网或反向代理鉴权之后；
- 定期更新依赖（`npm audit`）与 Docker 基础镜像。
