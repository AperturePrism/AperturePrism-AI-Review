# 本机（Windows + SMB 挂载）开发环境专用：SMB 不支持 symlink，
# 强制 npm copy workspace 包。CI 环境无 symlink 限制，此配置仅等效无害。
# This workspace lives on an SMB filesystem that does not support symlinks.
# Force npm to copy workspace/dependency packages instead of symlinking them.
install-links=true